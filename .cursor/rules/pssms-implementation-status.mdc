---
description: Honest implementation status — what works vs incomplete vs empty; finish design gaps here first
alwaysApply: true
---

# Implementation Status (do not pretend everything is done)

Foundation is strong (APIs + most domain libs + portals wired). Many design features are **partial** or **stubbed**. Prefer finishing these over inventing new product areas.

## Strong / largely real

- IAM dual auth, users/roles, enterprise master data, customers/contracts
- **§6 Module 6-A (Staff customer 360 · thin · smoked):** `GET /customers/:id/overview` (`customers.manage`) — org-scoped counts + samples (sites, contracts, employees, ACTIVE guards, invoices/outstanding, service requests, **complaints**, incidents, vehicles/permits, access 30d, pending visits); admin drawer `/superadmin/customers` **Customer 360** panel. **Smoke:** admin CUST-DEMO 200 (sites/contracts/guards/invoices); guard 403 missing `customers.manage`; portal 403 path denied; UI route 307→login. Deferred: full AR aging, mutate-from-360
- **§6 Module 6-B (Complaints · thin · smoked):** `CustomerComplaint` + migration `0024` (RLS); distinct from service-request tickets; lifecycle OPEN→ACKNOWLEDGED→UNDER_REVIEW→RESOLVED→CLOSED (+ cancel); creator≠processor SoD; portal `GET/POST /customers/me/complaints` + cancel OPEN; staff `GET/POST/PATCH /customers/complaints` (`customers.manage` OR `visitors.manage`); seed `CMP-00001`. **UI:** customer-web `/complaints`; Call Centre complaints table; 360 openComplaints. **Smoke:** portal list/create 201; staff path 403 for portal; callcentre ACK 200; self-ack `CREATOR_CANNOT_PROCESS`; admin ack 200; guard list 403; overview openComplaints≥1. Deferred: attachments, SMS/SLA timers, complaint→incident link
- **§6 Module 6-C (Customer reports · thin · smoked):** `GET /customers/:id/reports` (`customers.manage`) + portal `GET /customers/me/reports` — period window (default 30d; `from`/`to`); live counts (incidents, attendance, access, visitors, parking, complaints, tickets, invoices) + by-site breakdown + CSV download in UI; honest notes (no fake KPIs). **UI:** customer-web `/reports`; admin drawer Reports strip. **Smoke:** staff CUST-DEMO 200; bad period `INVALID_PERIOD`; portal 200; guard 403; UI `/reports` 307. Deferred: charts, PDF pack, SLA analytics beyond contract terms
- **§6 Module 6-D (Billing edit completeness · thin · smoked):** admin drawer Edit now patches commercial/billing (payment terms/method, SLA, invoice frequency, currency, credit limit, bank/account, taxExempt, account manager, VRN/region) via existing `PATCH /customers/:id` (`customers.manage`); view shows bank/credit/AM. api-client `UpdateCustomerBody` null-clearable. **Smoke:** admin PATCH CUST-DEMO persists NET_45/USD/bank/credit/taxExempt/AM then restore; guard 403; portal `CUSTOMER_PORTAL_READ_ONLY`. Deferred: attachments on profile, SLA analytics
- **§6 Module 6-E (Customer site create · thin · smoked):** `POST /customers/:id/sites` (`customers.manage`) — CRM wrapper over `SitesService` (forces `customerId` from path; branch/org + unique code; audit `site.created`). List still via `GET /customers/:id` `sites[]`. **UI:** `/superadmin/customers` drawer Add site modal (branch/code/name/address). api-client `createCustomerSite`. **Smoke:** marketing1 create → GET shows site; duplicate code conflict; admin OK; guard 403; portal read-only. Deferred: lat/long, branch modal optional customerId picker
- **§6 Module 6-F (Customer site edit/deactivate · thin · smoked):** `PATCH /customers/:id/sites/:siteId` (`customers.manage`) → `SitesService.update` (name/address/isActive; code/branch immutable; must belong to customer; audit `site.updated`). Customer `sites[]` includes `address`. **UI:** drawer Edit modal + Deactivate/Reactivate. api-client `updateCustomerSite`. **Smoke:** marketing1 rename+deactivate → GET; wrong customer/site 404; reactivate; guard 403; portal read-only. Deferred: branch reassign, enterprise PATCH route, lat/long
- **§6 Module 6-G (Customer employees register · thin · smoked):** `GET/POST /customers/:id/employees` (`customers.manage`) — CRM wrapper over `AccessControlService` (forces `customerId`; audit `access.employee.created`). **UI:** drawer roster + Add employee modal (name/number/email/phone/dept). api-client `listCustomerEmployees` / `createCustomerEmployee`. **Smoke:** marketing1 list/create 201; duplicate email 409; guard 403; portal read-only; overview count bumps. Deferred: (done in 6-I/6-K) portal IAM bind + invite, card/bio fields
- **§6 Module 6-H (Customer employee edit/deactivate · thin · smoked):** `PATCH /customers/:id/employees/:employeeId` (`customers.manage`) → `AccessControlService.updateEmployee` (name/number/email/phone/dept/isActive; must belong to customer; email unique; audit `access.employee.updated`). **UI:** drawer Edit modal + Deactivate/Reactivate. api-client `updateCustomerEmployee`. **Smoke:** marketing1 rename+deactivate → list; wrong customer 404; reactivate; guard 403; portal read-only. Deferred: (done in 6-K) card/bio; access.manage PATCH route still optional
- **§6 Module 6-I (CUSTOMER_EMPLOYEE invite + bind · thin · smoked):** `POST /customers/:id/employees/:employeeId/invite-portal` (`customers.manage`) — creates User `CUSTOMER_EMPLOYEE` + `customerId` + `mustChangePassword`, binds `CustomerEmployee.userId`; requires employee email + active + not already linked; one-time temp password; EMAIL `CUSTOMER_EMPLOYEE_INVITE`; audit `access.employee.portal_invited`. **UI:** drawer Invite portal + password-once strip. api-client `inviteCustomerEmployeePortal`. **Smoke:** invite EMP-1002 → login MFA-free with temp → `mustChangePassword` / `GET /access/me` 200; re-invite `ALREADY_LINKED`; no-email 400; Jane already linked 409; guard 403. Deferred: link existing User-only, SMS/WhatsApp
- **§6 Module 6-J (Employee deactivate ↔ portal login · thin · smoked):** `AccessControlService.updateEmployee` when `isActive` flips and `userId` bound to same-customer `CUSTOMER_EMPLOYEE` User → sets `User.isActive` (+ `suspendedReason=Customer employee deactivated` / clear on reactivate); audit after includes `portalLoginSynced`. **UI:** roster shows “portal login suspended” when inactive+linked. **Smoke:** deactivate EMP-1002 → login 401; reactivate → login 200; unlinked deactivate no user side-effect; guard 403. Deferred: IAM approval for external suspend, revoke refresh tokens live
- **§6 Module 6-K (Access card / biometric refs · thin · smoked):** CRM create/update/list expose `accessCardRef` / `biometricRef` (existing Prisma + access DTO fields) via `POST/PATCH /customers/:id/employees*`. **UI:** Add/Edit employee modals + roster chips. **Smoke:** marketing1 create with card/bio → list; PATCH clear bio; guard 403; UI `/superadmin/customers` 307. Deferred: device enroll UX, org-unique card constraint, RFID ingest match UI
- **§6 Module 6-L (Assigned guards roster · thin · smoked):** `GET /customers/:id/guards?status=ACTIVE|ENDED|ALL` (`customers.manage`) — site-scoped `GuardDeployment` roster (cap 200) + guard/site/contract enrich; read-only (deploy/end stays Branch Ops). **UI:** drawer Assigned guards + Active/All toggle + Ops link. api-client `listCustomerAssignedGuards`. **Smoke:** marketing1 ACTIVE ≥1 on CUST-DEMO; ALL; guard 403; UI 307. Deferred: CRM deploy mutate
- **§6 Module 6-M (Contacts directory · thin · smoked):** `CustomerContact` + migration `0025` (RLS); roles GENERAL/BILLING/OPERATIONS/SECURITY/OTHER; `GET/POST/PATCH /customers/:id/contacts` (`customers.manage`); portal `GET /customers/me/contacts` (active only); primary clears others + syncs `contactPerson`/`contactDesignation` (+ phone/email); audit create/update. Seed Jane + billing Asha on CUST-DEMO. **UI:** drawer Contacts + Add/Edit/Deactivate. **Smoke:** marketing1 list/create/primary-swap; portal 200; portal POST 403; guard 403; UI 307. Deferred: (done in 6-N) portal contacts page; org-unique email
- **§6 Module 6-N (Portal contacts page · thin · smoked):** customer-web `/contacts` read-only via `listCustomerPortalContacts` → `GET /customers/me/contacts`; nav Account → Contacts (`contracts.manage`); profile link to directory; role filter + search. **Smoke:** portal GET 200 (N≥1 primary); Jane employee `CUSTOMER_EMPLOYEE_PATH_DENIED`; UI `/contacts` 307 (local next — Docker Desktop stuck mid-recreate). Deferred: portal self-edit contacts, org-unique email
- **§8 Module 8-A (Guard status lifecycle · thin · smoked):** `PATCH /guards/:id/status` enforces transitions (ACTIVE/AVAILABLE/ON_LEAVE/SUSPENDED/TRANSFERRED; TERMINATED terminal); ON_LEAVE/TRANSFERRED/SUSPENDED/TERMINATED force not-deployable + end active deployments; AVAILABLE ends deployments + defaults deployable; response `allowedNextStatuses` + `canToggleDeployable`. **UI:** `/operations/guards` status select, filters, KPIs Available/On leave. **Smoke:** ops1 ACTIVE→ON_LEAVE (elig false); ON_LEAVE→TRANSFERRED 400; →AVAILABLE elig true; restore ACTIVE; guard 403; UI `/operations/guards` 307. Deferred: (done in 8-D) ABSENT; medical/uniform → 8-B/C; performance, Employment→CEO
- **§8 Module 8-B (Medical fitness + ID ref · thin · smoked):** `GuardProfile.medicalFitnessVerified` / `medicalFitnessExpiry` / `nationalIdRef` (migration `0026`); `PATCH /guards/:id/readiness` extended; list DTO; Readiness OK = training+clearance+medical (still no deploy hard-block). Seed GRD-0001 medical+NIDA. **UI:** drawer medical + national ID fields. **Smoke:** ops1 seed GRD-0001 medical; PATCH GRD-0004 medical+NIDA; clear ID null; guard 403; UI `/operations/guards` 307. Deferred: MinIO medical docs, performance
- **§8 Module 8-C (Uniform / equipment issued · thin · smoked):** `uniformIssued` / `equipmentIssued` (migration `0027`) via same readiness PATCH; checklist only (asset register remains `/assets`). Seed GRD-0001 both true. **UI:** drawer kit checkboxes + Assets link. **Smoke:** ops1 seed true; PATCH GRD-0004 kit on; unset equipment; guard 403; UI `/operations/guards` 307. Deferred: auto-sync from AssetAssignment, performance reviews
- **§8 Module 8-D (ABSENT status · thin · smoked):** `GuardStatus.ABSENT` (migration `0028`); transitions from ACTIVE/AVAILABLE/ON_LEAVE; returns via ACTIVE/AVAILABLE/ON_LEAVE/SUSPENDED/TERMINATED; force not-deployable + end deployments. **UI:** Absent filter/KPI/badge/select. **Smoke:** ops1 ACTIVE→ABSENT (elig false); ABSENT→ACTIVE; ABSENT→TRANSFERRED 400 `INVALID_GUARD_STATUS_TRANSITION`; guard 403; UI `/operations/guards` 307. Deferred: (done in 8-E) attendance close; performance reviews, MinIO medical docs
- **§8 Module 8-E (ABSENT closes open punch · thin · smoked):** `AttendanceService.closeOpenForGuardAbsent` via Attendance→Guards bridge (no Nest cycle); status→ABSENT SUPERVISOR clock-out + `STATUS_ABSENT` remark + optional `reason`; response `closedAttendanceIds`; audit `attendance.closed_for_absent`. **UI:** prompt reason + teal notice when punches closed. **Smoke:** ops1 supervisor-clock-in GRD-0004 → ABSENT closes punch + reason in remarks; ABSENT with no open → no `closedAttendanceIds`; guard 403; UI `/operations/guards` 307. Deferred: create absent-day record when no open punch; (alertness cancel → 8-F); supervisor-mobile reason UX
- **§8 Module 8-F (ABSENT cancels alertness · thin · smoked):** `AlertnessStatus.CANCELLED` (migration `0029`); `cancelScheduledForGuardAbsent` via ABSENT duty cleanup bridge; response `cancelledAlertnessIds`; audit `alertness.cancelled_for_absent`; mark-missed/confirm blocked on CANCELLED. **UI:** notice includes cancelled check count. **Smoke:** ops1 schedule + clock-in GRD-0004 → ABSENT closes punch + cancels SCHEDULED (incl. auto-scheduled); pending empty; mark-missed 400; no-sched ABSENT → no cancel ids; guard 403; UI `/operations/guards` 307. Deferred: absent-day create; (duty block → 8-G); MinIO medical, Employment→CEO
- **§8 Module 8-G (ABSENT blocks new duty · thin · smoked):** clock-in / supervisor-clock-in / device clock-in reject `ABSENT` (+ existing SUSPENDED/TERMINATED) with `GUARD_STATUS_BLOCKS_DUTY`; manual alertness schedule `GUARD_STATUS_BLOCKS_ALERTNESS`; `scheduleForDuty` skips blocked statuses. **UI:** branch attendance pickers exclude blocked; help blurb. **Smoke:** ops1 ABSENT → supervisor-clock-in 400 `GUARD_STATUS_BLOCKS_DUTY`; schedule 400 `GUARD_STATUS_BLOCKS_ALERTNESS`; ACTIVE clock-in 201; UI `/operations/guards` + `/branch/attendance` 307. Deferred: absent-day create, MinIO medical, Employment→CEO
- **§10 Module 10-A (LATE confirm · thin · smoked):** confirm after `scheduledAt` (+ `ALERTNESS_LATE_GRACE_MINUTES`, default 0) → `AlertnessStatus.LATE` (still success; MISSED remains scan/mark path); pending DTO `pastDue`; audit `late`. **UI:** branch overdue hint; guard-app past-due warning. **Smoke:** ops1 schedule past for GRD-0001 → pending `pastDue`; guard1 confirm → LATE; future confirm → CONFIRMED; mark-missed → MISSED; UI `/branch/attendance` 307. Deferred: (done in 10-B) supervisor remarks; deviceId, photo/selfie/task methods, payroll/discipline hooks, SLA policy master, full history roster
- **§10 Module 10-B (Supervisor remarks on miss · thin · smoked):** `AlertnessCheck.supervisorRemarks` (migration `0030`); `POST …/alertness/:id/missed` body `supervisorRemarks`; FieldAlert message + outbox/audit include note. **UI:** Mark missed prompt on `/branch/attendance`. **Smoke:** ops1 miss with remarks persists + FieldAlert message; guard 403; miss without remarks OK; UI `/branch/attendance` 307. Deferred: deviceId, photo/selfie/task methods, payroll/discipline hooks; (history → 10-C)
- **§10 Module 10-C (Alertness history roster · thin · smoked):** `GET /attendance/alertness/history` — completed statuses (CONFIRMED/LATE/MISSED/CANCELLED default); filters guard/site/status/from/to/take; enrich employeeNumber/siteCode/siteName; site-scoped staff + guard self. **UI:** `/branch/attendance` today history + status chips. **Smoke:** ops1 LATE row enriched SITE-WAREHOUSE-A/GRD-0001; `status=LATE`; bad status 400; guard self OK; guard other guardId 403; UI `/branch/attendance` 307. Deferred: deviceId, photo/selfie/task, payroll hooks, multi-day export
- **§11 Module 11-A (Employee self check-in/out · thin · smoked):** `POST /access/me/entries` (`access.self`) — forces `employeeId`/`customerId` from JWT-bound `CustomerEmployee`; site must belong to own customer; optional `entryType` (else toggle); default method `QR`; idempotent `clientEventId`; audit `via:self`. Portal allowlist: employee POST + `GET /customers/me/sites`. **UI:** customer-web `/my-access` site picker + Check in/out. api-client `recordMyAccessEntry`. **Smoke:** Jane CHECK_IN/OUT 201 + enrich; bad site `INVALID_SITE`; portal admin `CUSTOMER_PORTAL_READ_ONLY`; guard missing `access.self` 403; list own entries. Deferred: device bio/card UX, (accessLevel → 11-B), (site grants → 11-C), (gate picker → 11-D)
- **§11 Module 11-B (accessLevel · thin · smoked):** `AccessLevel` enum STANDARD/RESTRICTED/ELEVATED (migration `0031`); on `CustomerEmployee` default STANDARD; create/update/list/me via access + CRM `/customers/:id/employees*`; seed Jane STANDARD, EMP-1002 ELEVATED, EMP-1003 RESTRICTED. **UI:** admin drawer level select + roster chip; customer-web `/my-access` + `/access` show level. **Smoke:** marketing1 list shows levels; PATCH Jane→ELEVATED then restore STANDARD; Jane `/access/me` level; bad enum 400; guard 403; UI `/superadmin/customers` 307. Deferred: (site grants → 11-C), enforce level at gate/device, device bio/card UX
- **§11 Module 11-C (Site grants · thin · smoked):** `CustomerEmployeeSiteAccess` (migration `0032`, RLS); empty grants = unrestricted all active customer sites; non-empty = self check-in + `/access/me/sites` filtered. CRM `GET/PUT /customers/:id/employees/:employeeId/sites` (`customers.manage`); audit `access.employee.sites_updated`; self `SITE_NOT_GRANTED` on punch. Seed Jane → SITE-WAREHOUSE-A only. **UI:** admin Sites modal; `/my-access` uses granted picker. **Smoke:** Jane me/sites warehouse only; CHECK_IN warehouse 201; office `SITE_NOT_GRANTED`; marketing1 PUT add office then clear unrestricted; guard 403; portal PUT 403; UI 307. Deferred: (gate picker → 11-D), enforce on staff `POST /entries`, device bio/card
- **§11 Module 11-D (Gate picker · thin · smoked):** `GET /access/me/sites` embeds active `gates[{id,code,name}]` per site; self check-in validates `gateId` on site + `isActive` (`INVALID_GATE`); entry DTO/list enrich `gateCode`/`gateName`. **UI:** `/my-access` Gate select (defaults first gate). **Smoke:** Jane me/sites GATE-MAIN+VEHICLE; CHECK_IN with GATE-MAIN 201 + gateCode; wrong-site/random gate `INVALID_GATE`; no-gate still OK; UI `/my-access` 307. Deferred: enforce on staff `POST /entries`, device bio/card UX
- **§12 Module 12-A (Gate deny → FieldAlert · thin · smoked):** on `POST /visitors/gate/verify` deny, creates FieldAlert `VISITOR_GATE_DENIED` (severity HIGH for blacklist/revoked/already-used else MEDIUM; stage SUPERVISOR); outbox `field.alert.created`; audit `visitor.gate.deny_alerted`; response `fieldAlertId` (null on allow / idempotent replay). **UI:** Call Centre gate entries note + deny reason col; `/branch/alerts` description; gate-app result “Ops FieldAlert raised”. **Smoke:** gate1 bad code → DENIED_INVALID + fieldAlertId; list field-alerts contains VISITOR_GATE_DENIED; allow path no alert; replay clientEventId no second alert; UI `/callcentre` + `/branch/alerts` 307. Deferred: **(exit punch → 12-B; code delivery → 12-C; ID refs → 12-D; host notify → 12-E)**
- **§12 Module 12-B (Visitor gate exit punch · thin · smoked):** `VisitorEntry.direction` IN|OUT (migration `0033`, default IN); `POST /visitors/gate/exit` (`visitors.manage`) — lookup by appointmentId|referenceNumber|verificationCode (even if used)|IN entryId; creates ALLOWED OUT; audit `visitor.gate.exited`; appointment COMPLETED on exit (entry verify keeps APPROVED); errors `NO_OPEN_VISIT` / `ALREADY_EXITED`; no FieldAlert on exit. Entry list DTO includes `direction`. **UI:** gate-app Entry/Exit mode + duty Exit button; Call Centre Dir badge; api-client `gateExit`. **Smoke:** approve→verify IN APPROVED→exit OUT COMPLETED; second exit 400; guard 403; UI `/callcentre` 307. Deferred: **(code delivery → 12-C; ID refs → 12-D; host notify → 12-E)**
- **§12 Module 12-C (Email/WhatsApp gate code delivery · thin · smoked):** on host approve, enqueue `VISITOR_GATE_CODE` to EMAIL (if `visitorEmail`), SMS + WHATSAPP (if phone) via `NotificationsService.enqueueVisitorGateCode` + outbox (console adapters OK); response `delivery:{email,sms,whatsapp}`; audit `visitor.code.delivery_queued`; no contact → approve + code still, delivery all false; idempotency per channel+appointment. **UI:** customer-web `/visitors` + Call Centre approve modal channel hints. **Smoke:** book email+phone → approve delivery all true + EMAIL notif; no-contact delivery false; guard 403; UI `/visitors` 307. Deferred: real Meta WhatsApp API; **(ID refs → 12-D; host notify → 12-E)**
- **§12 Module 12-D (Visitor ID document refs · thin · smoked):** `VisitorIdType` + `idType`/`idNumber` on `VisitorAppointment` (migration `0034`); public/staff create both-or-neither (`ID_INCOMPLETE`); appointment + gate verify/entry DTOs expose refs; seed VIS-DEMO-001 NIDA. **UI:** visitor-web book ID type/number; customer-web `/visitors` chip; Call Centre ID col; gate-app allow shows ID. **Smoke:** book NIDA+number 201; idNumber-only 400; no-ID 201; approve+verify includes id; UI 307; guard gated. Deferred: **(host notify → 12-E; MinIO ID scan → 12-F)**
- **§12 Module 12-E (Host notify on gate deny · thin · smoked):** on `POST /visitors/gate/verify` deny when `appointmentId` known (matched code — not pure invalid / blacklist-without-appt), resolve `hostUserId` → User phone/email (same org); enqueue `VISITOR_GATE_DENIED_HOST` SMS + EMAIL via `NotificationsService.enqueueVisitorGateDeniedHost`; idempotency `visitor-deny-host-{sms|email}-{entryId}`; audit `visitor.gate.host_notified` when any channel queued; response `hostNotified:{sms,email}` (null when no appointment). Seed `portal@` phone `+255755000200`. **UI:** gate-app “Host notified”; Call Centre deny help text; api-client `GateVerifyResponse`. **Smoke:** book+approve host=portal → already-used deny → fieldAlertId + hostNotified sms+email + PENDING notifs; random invalid → fieldAlertId + hostNotified null; guard entries 403; UI `/callcentre` 307. Deferred: real Meta WhatsApp API; **(MinIO ID scan → 12-F)**
- **§12 Module 12-F (Visitor ID scan via MinIO · thin · smoked):** `DocumentObject` `resourceType=VisitorAppointment` on existing `libs/documents` + MinIO `pssms-documents` (no parallel table); parent perm `visitors.manage`; org ownership by appointment id; portal CUSTOMER_PORTAL **list/presign** when `appointment.customerId === jwt.customerId` (upload blocked — `CUSTOMER_PORTAL_READ_ONLY`); staff upload `documents.manage` + `visitors.manage`. **UI:** Call Centre Docs modal (upload/list/presign); customer-web `/visitors` ID scans modal (read-only). Public visitor-web book-time upload skipped (no auth). **Smoke:** callcentre1 upload png VIS-DEMO-001 → 201; list+presign 200; guard missing `visitors.manage` 403; gate1 missing `documents.manage` 403; portal list 200 / upload 403; bad appointment 404; UI `/callcentre` + `/visitors` 307. Deferred: virus scan, public book-time upload, gate-app download UX / `hasIdScan` flag, real Meta WhatsApp API
- **§13 Module 13-A (Vehicle RFID / tag refs · thin · smoked):** `Vehicle.rfidTagRef` (migration `0035`, org-unique when set); create/list/PATCH `/parking/vehicles/:id` expose tag (`parking.manage`); duplicate → 409 `RFID_TAG_IN_USE`; audit `parking.vehicle.updated`; owner `GET /parking/me` includes tag; optional `POST /parking/entries` `rfidTagRef` resolves plate/vehicleId (ALLOW if active + ACTIVE permit at site). Seed T123ABC → `RFID-DEMO-T123`. **UI:** parking-web `/vehicles` (Access nav) + Edit RFID modal; owner `/owner` RFID chip. **Smoke:** parking1 list/PATCH set/clear + dup 409; owner1 me sees tag / ops PATCH 403; guard 403; UI `/vehicles` + `/owner` 307. Deferred: stall maps; MQTT RFID barriers; Nest video
- **§13 Module 13-B (Parking permit fee / billing · thin · smoked):** `ParkingPermit.feeAmount`/`currency`/`invoiceId`/`billedAt` (migration `0036`); create/`PATCH /parking/permits/:id` fee; approve does **not** auto-bill; `POST …/permits/:id/bill` (`parking.manage`) → DRAFT invoice via `InvoicesService` (ParkingModule→FinanceModule; no finance repo in parking); requires ACTIVE + fee>0 + vehicle.customerId; `ALREADY_BILLED` / `CUSTOMER_REQUIRED_FOR_BILLING`; audit `parking.permit.billed`; DTO + invoiceNumber enrich; seed PRM-DEMO-001 fee 150000 TZS. **UI:** parking-web `/permits` Edit fee + Bill; owner `/owner` fee/invoice chips (read-only). **Smoke:** parking1 fee→bill DRAFT + invoiceId; second bill 409; no-customer vehicle 400; owner bill 403; guard 403; UI `/permits` 307. Deferred: stall maps; MQTT RFID barriers; Nest video; auto-send invoice; permit renewals billing
- **§4 User Categories Phase 1 (ops hierarchy):** roles `FIELD_OFFICER` / `BRANCH_MANAGER` / `OPERATIONS_MANAGER` seeded (restricted perms — not allPerms) + demos `field1@` / `bom1@` / `ops1@` (`ChangeMe123!`); AL1 escalate **stage-gated** (Supervisor→FIELD, Field→BOM, BOM→CONTROL; higher roles may escalate lower stages; Guard-only 403); attendance approve/supervisor-punch + incident staff/close allowlists include the three roles (non-CRITICAL close: BOM/Ops Mgr); Keycloak realm users listed (AuthZ still Prisma). **A4:** guard roster admin via `guards.manage` (BOM + Ops Mgr) — see Harden A4
- **§4 User Categories Phase 2 (people & money):** `ACCOUNTS_OFFICER` (`accounts1@`) — `finance.manage` + customers/contracts/docs/approvals; `PAYROLL_OFFICER` (`payroll1@`) — `payroll.manage` + approvals/ESS (no HR/finance admin); `HR_OFFICER` **hardened** off allPerms → `hr.manage`/`ess.access`/`approvals.act`/`documents.manage`/`loans.manage` + demo `hr1@` (seed uses `restrictedPermSets` map). Invoice **list** gated `finance.manage` (customer portal still works — portal role has that perm). **Honest:** Accounts has full customers/contracts mutate (not picker-only); staff `GET /customers` + `GET /contracts` still ungated; loan-approval step remains GM (HR can create/manage loans, not final-approve)
- **§4 User Categories Phase 3 (procurement & store):** `PROCUREMENT_OFFICER` (`procurement1@`) — `procurement.manage` + `inventory.manage` + approvals/docs; `STOREKEEPER` (`store1@`) — `assets.manage` + `inventory.manage` + ESS/docs (returns confirm SoD creator≠confirmer). **API gates:** procurement suppliers/POs → `procurement.manage`; inventory → `inventory.manage`; supplier portal stays read-only via existing `SupplierPortalGuard` (me + scoped PO list only — mutate 403)
- **§4 User Categories Phase 4 (monitoring):** `CCTV_OPERATOR` / `CONTROL_ROOM` seeded; AL1/attendance/guard admin use **positive role allowlists**. **A2:** CCTV uses `cctv.manage` (not Field `operations.manage`) — see Harden A2 bullet
- **§4 User Categories Phase 5 (governance):** `DEPARTMENT_HEAD` (`depthead1@`) — approvals/HR/ESS/reporting/enterprise/audit; workflows **transfer / exit** Dept Head → GM (**leave** → Harden A5 full matrix); `CISO` (`ciso1@`) — compliance + audit + users.manage + approvals (no finance/ops; **A6 elevation ceiling**); `INTERNAL_AUDITOR` (`auditor1@`) — `audit.read` + docs only (**no** `approvals.act` / finance / `compliance.manage`); compliance **GET** allowlisted via `audit.read` OR `compliance.manage`, mutates stay `compliance.manage`
- **§4 User Categories Phase 6 (support):** `CALL_CENTRE` (`callcentre1@`) — `visitors.manage` + docs (visitor inbox + service tickets via visitors OR customers allowlist — no full CRM); `IT_SUPPORT` (`it1@`) — `users.manage` + notifications/audit/docs (helpdesk, not integrator); `DEVELOPER` **refined** (`dev1@`) — drop `users.manage` (integrations + notifications + audit + ops for device summary). **API:** authenticated visitor staff routes gated `visitors.manage` (public book + customer portal still OK — portal has that perm)
- **§4 Harden A1 (DPO split):** role `DPO` + demo `dpo1@` — perm `dpo.manage` (breach register mutate); `COMPLIANCE_OFFICER` renamed (policies + `approvals.act`; breach **GET** only). CISO keeps both `compliance.manage` + `dpo.manage`. **API:** `POST/PATCH /compliance/breaches` → `dpo.manage`; GET tightened (dpo/compliance/CISO/GM/SuperAdmin/Internal Auditor — not bare `audit.read` for IT_SUPPORT). UI policies+breaches mutate buttons gated. Deferred §32: consent/DPIA/DR, full DPO vs CO matrix on archive
- **§4 Harden A2 (`cctv.manage`):** perm `cctv.manage`; `CCTV_OPERATOR` (`cctv1@`) **drops** `operations.manage` (no Field/Branch/Guards nav); CONTROL_ROOM + Ops Mgr gain `cctv.manage`. **API:** device list/get/events/register/update allow ops OR cctv; CCTV-scoped forced to `CCTV_CAMERA` / `CCTV_EVENT`; **no** `directPush` API keys for CCTV-scoped; gateways + commands stay ops; ANPR list allow parking|cctv|ops, decide parking|ops only. Nav `/cctv` → `cctv.manage`. Deferred: NVR SDK, Nest video, MQTT, full parking controller RBAC
- **§4 Harden A3 (Guard own-records):** `GUARD` drops `operations.manage` (no Field/Branch/Guards admin nav); keeps attendance/incidents/ESS/docs. `isGuardSelfScoped` filters attendance / field-alerts / patrol scans by `guardId`, incidents by `reporterId`, EOB by `officerId`; staff alertness schedule/miss/scan + EOB correct blocked / self-only; pending manage-mode skipped. EOB create/list allow `attendance.manage` (approve stays ops). Deferred: runtime branch expand, CISO elevation
- **§4 Harden A4 (`guards.manage`):** mutate create/status/readiness/linkable → `guards.manage`; **GET list** `operations.manage` OR `guards.manage` (branch pickers). Seed on Ops Mgr + BOM; SUPERVISOR/FIELD/CONTROL_ROOM/CCTV/GUARD **no** admin. Nav `/operations/guards` → `guards.manage`; ops console Suspend/Manage gated. Deferred: HR create profiles, site-scoped roster ABAC on list
- **§4 Harden A5 (Leave matrix):** `leave-approval` **Supervisor → HR → Dept Head → GM**; supervisor gains `approvals.act` (not `hr.manage`); approve/reject `hr.manage` OR `approvals.act`; leave list DTO enriched with step/role; HR + `/approvals` gate actions on `requiredRole`; LeaveRequest acts via domain API from approvals queue; generic `approvals.act` terminal sync mirrors Contract. Transfer/exit stay DeptHead→GM. Deferred: supervisor leave roster UI outside `/approvals`, FIELD as alternate step-1
- **§4 Harden A6 (CISO elevation ceiling):** `PRIVILEGED_ASSIGNMENT_ROLES` = SUPER_ADMIN/GM/CEO/CMD/CISO; bypass SUPER_ADMIN+GM only. CISO/IT `users.manage` cannot assign privileged roles, cannot setRoles/suspend/reactivate privileged accounts, cannot grant `users.manage` on custom roles. `RolesController` gated `users.manage`. Deferred: finer ICT vs CISO split
- **§5 Module 5-A (Users console · thin · smoked):** admin-web `/superadmin/users` + nav `users.manage` — list/create/set-roles/suspend/reactivate on existing `GET/POST /users`, `PATCH …/roles|suspend|reactivate` + `GET /roles`; User DTO enriched phone/lastLogin/suspend fields; api-client helpers. Picker excludes bound external (customer/supplier/OSC/owner/contractor/consultant) + A6 privileged unless SUPER_ADMIN/GM. **Smoke:** admin/it1 list 200; guard 403; create/roles/suspend→login 401/reactivate `{}` 200; UI `/superadmin/users` 200. Deferred: (password policy → M5-K; mustChangePassword on create → M5-H)
- **§5 Module 5-B (Login history · thin · smoked):** `GET /users/login-history?userId=&success=&take=` (`users.manage`, org-scoped via user.organizationId, take 1–200); reads existing `iam.login_history` (auth already writes success/fail). api-client `listLoginHistory`; UI modal on `/superadmin/users` (All/Success/Failed + per-user Logins). **Smoke:** admin/it1 200; guard 403; `success=false` after bad password; userId filter. Deferred: read-audit of IP/UA views, PG RLS on login_history
- **§5 Module 5-C (MFA TOTP · thin · smoked):** existing `POST /auth/mfa/setup|enable|disable` + login `mfaCode`; added `GET /auth/mfa/status`; setup blocked when already enabled; exception filter preserves app codes (`MFA_REQUIRED`/`MFA_INVALID_CODE`). api-client `login(…, mfaCode?)` + `getMfaStatus/setupMfa/enableMfa/disableMfa`. **UI:** admin login shows authenticator field after `MFA_REQUIRED`; ESS `/ess/security` enroll/disable (setup key + otpauth link). **Smoke:** it1 setup→enable→login without code `MFA_REQUIRED`→with code 200→wrong `MFA_INVALID_CODE`→setup-while-on 400→disable→login ok. Deferred: QR canvas, recovery codes, org force-MFA policy, session revoke on enable, MFA challenge on executive/customer/supplier/parking logins, encrypt mfaSecret at rest (admin MFA reset → M5-J)
- **§5 Module 5-D (Site/branch ACL · thin · smoked):** `GET/PUT /users/:id/access` (`users.manage`) replaces `UserBranchAccess` + `UserSiteAccess`; org-validated IDs; catalog embedded (no ops perm); rejects external party-bound roles; A6 privileged ceiling on GET+PUT; audit `IDENTITY_USER_ACCESS_CHANGED`. api-client `getUserAccess`/`setUserAccess`. **UI:** `/superadmin/users` Sites modal (branch + direct site checkboxes; external + non-GM privileged disabled). JWT expands via A7 on next login/refresh. **Smoke:** admin/it1 GET staff 200; guard 403; owner `EXTERNAL_USER_ACL`; PUT site swap + restore; it1→CEO GET/PUT 403; bad site `INVALID_SITE_IDS`. Deferred: live re-expand without refresh, ACL-change via approval
- **§5 Module 5-E (IAM role-change approval · thin · smoked):** `IamChangeRequest` + migration `0021`; workflow `iam-role-change-approval` (GM step); `POST /users/:id/roles/submit` + list/approve/reject; `PATCH …/roles` break-glass SUPER_ADMIN/GM only (`USE_ROLE_CHANGE_SUBMIT` for IT); creator≠approver; ApprovalsService terminal sync + `/approvals` domain route; **stale guard** cancels if live roles ≠ submit snapshot (`STALE_ROLE_CHANGE` / CANCELLED). **UI:** users Roles → Submit for approval; pending strip; GM on `/approvals`. **Smoke:** it1 PATCH 403 code; submit PENDING; it1 approve `CREATOR_CANNOT_APPROVE`; guard list 403; GM approve applies roles; GM reject unchanged; break-glass mid-flight → approve cancels stale (roles kept). Deferred: SysAdmin→ICT→CISO→CEO→CMD matrix, ACL-via-approval, apply-via-identity-port only
- **§5 Module 5-F (Suspend-via-approval · thin · smoked):** `changeType=SUSPEND` + `reason` (migration `0022`); `POST /users/:id/suspend/submit`; `PATCH …/suspend` break-glass SUPER_ADMIN/GM only (`USE_SUSPEND_SUBMIT`); same GM workflow + creator≠approver; stale if already inactive (`STALE_SUSPEND` / CANCELLED). **UI:** Suspend → Submit for approval (IT) / Suspend now (GM/SA); pending strip shows SUSPEND. **Smoke:** it1 PATCH 403; submit PENDING; self-approve blocked; GM approve → login 401; GM reject keeps active; break-glass mid-flight → CANCELLED. Deferred: full SysAdmin→CMD matrix
- **§5 Module 5-G (Reactivate-via-approval · thin · smoked):** `changeType=REACTIVATE`; `POST /users/:id/reactivate/submit`; `PATCH …/reactivate` break-glass SUPER_ADMIN/GM only (`USE_REACTIVATE_SUBMIT`); same GM workflow + creator≠approver; stale if already active (`STALE_REACTIVATE` / CANCELLED); ApprovalsService terminal sync + domain finalize fallback (no empty-role wipe). **UI:** Request reactivate (IT) / Reactivate (GM/SA); pending strip shows REACTIVATE. **Smoke:** it1 PATCH 403; submit PENDING; self-approve blocked; GM approve → login 201; GM reject keeps inactive; break-glass mid-flight → CANCELLED. Deferred: full SysAdmin→CMD matrix
- **§5 Module 5-H (mustChangePassword on admin create · thin · smoked):** `POST /users` sets `mustChangePassword=true`; global `MustChangePasswordGuard` allows only `GET /auth/me` + `POST /auth/change-password` until cleared; User DTO exposes flag. **UI:** admin `/change-password` + login/middleware/portals gate; create modal notes temp password; roster “Must change password”. **Smoke:** create → login `mustChangePassword`; `GET /users` 403 `MUST_CHANGE_PASSWORD`; change-password → flag false + APIs 200; seed users unchanged. Deferred: (org password policy → M5-K)
- **§5 Module 5-I (Admin password reset · thin · smoked):** `POST /users/:id/reset-password` (`users.manage`); sets temp password + `mustChangePassword=true`; cannot reset self (`CANNOT_RESET_OWN_PASSWORD`); A6 privileged ceiling; policy check; audit `IDENTITY_PASSWORD_RESET`. **UI:** Reset PW modal on `/superadmin/users`. **Smoke:** it1→marketing reset + login forced; it1→self 403; it1→CEO 403; guard 403; change-password clears flag. Deferred: email delivery of temp password (MFA clear → M5-J; org policy → M5-K)
- **§5 Module 5-J (Admin MFA reset · thin · smoked):** `POST /users/:id/mfa/reset` clears TOTP (`mfaEnabled`/`mfaSecret`/`mfaVerifiedAt`); cannot reset self (`CANNOT_RESET_OWN_MFA`); A6; audit `IDENTITY_MFA_ADMIN_RESET`; password reset also clears MFA. User DTO `mfaEnabled`. **UI:** Reset MFA + roster “MFA on”. **Smoke:** enable MFA on marketing → admin reset → login without code; self-reset 403; it1→CEO 403; guard 403; password-reset clears MFA. Deferred: org force-MFA policy, recovery codes, encrypt mfaSecret at rest
- **§5 Module 5-K (Org password policy · thin · smoked):** `Organization.passwordPolicy` JSON (migration `0023`); `GET/PUT /users/password-policy` + `GET /auth/password-policy`; PUT SUPER_ADMIN/GM only (`PASSWORD_POLICY_DENIED`); create/reset/change-password + portal invite enforce resolved policy; audit `IDENTITY_PASSWORD_POLICY_UPDATED`. **UI:** Password policy modal on `/superadmin/users`; change-password shows summary. **Smoke:** it1 GET defaults; it1 PUT 403; GM PUT minLength=12; create with 10-char fails WEAK_PASSWORD; restore defaults. Deferred: full SysAdmin→CMD matrix, per-role policy overrides
- **§4 Harden A7 (runtime branch→sites):** `AuthService` login/refresh/`me` unions `UserSiteAccess` with all org sites under `UserBranchAccess` branches into JWT `allowedSiteIds`. Seed `bom1` = branch-only (legacy site rows cleared). Site-only users unchanged; empty both = fail-closed. Deferred: live expand without token refresh, `branches.list` filter, staff customer/contract list gates
- **§4 External E1 (Customer Employee Access · Portal 35.9):** role `CUSTOMER_EMPLOYEE` + perm `access.self`; `CustomerEmployee.userId` (migration `0017`); demo `jane.doe@demo-mfg.co.tz` → EMP-1001 + CUST-DEMO. **API:** `GET /access/me`; entries self-scoped; roster/POST need `access.manage`; portal guard employee allowlist. **UI:** customer-web `/my-access` + filtered nav. **Self check-in → 11-A; accessLevel → 11-B; site grants → 11-C; gate picker → 11-D.** Deferred: device bio/card UX, invite link UX, separate employee-access app
- **§4 External E2 (Other Security Company · Portal 35.14 / §15 thin · smoked):** role `OTHER_SECURITY_COMPANY` + perm `recruitment.b2b`; models `B2bSecurityPartner` / `GuardSupplyRequest` (migration `0018`); JWT `b2bPartnerId` + `OtherSecurityPortalGuard`; demo `partner@demo-security.co.tz` → OSC-DEMO + `GSR-00001`. **API:** `GET …/b2b/partners/me`, `POST/GET …/b2b/requests`, staff `PATCH …/status` (`recruitment.manage`; HR_OFFICER gained it; creator≠processor). **Smoke:** partner own-scope + create; staff postings/triage 403; guard list 403; HR triage UNDER_REVIEW; public careers 200; create OSC without partner 400. **UI:** recruitment-web `/partner/login` + `/partner/requests`; admin `/hr/b2b-requests`. Deferred: B2B invoice, screening→hire, dedicated app/port, partner invite UX
- **§4 External E3 (Vehicle owner/driver self-view · thin · smoked):** role `VEHICLE_OWNER` + perm `parking.self`; `Vehicle.userId` (migration `0019`); `VehicleOwnerPortalGuard` allowlist `/parking/me|/me/permits|/me/entries` + auth me/change-password; demo `owner1@highlink.co.tz` → T123ABC (Jane stays CUSTOMER_EMPLOYEE). **API:** owner self GETs; ops parking routes gated `parking.manage` (ANPR still parking|cctv|ops). **Smoke:** owner T123ABC + permits/entries 200; ops vehicles/blacklist/approve 403; Jane/guard `/parking/me` 403; `parking1` ops still 200. **UI:** parking-web `/owner/login` + `/owner` (separate owner cookies). Deferred: billing, ANPR decide for owners, invite UX, new subdomain/app
- **§4 External E4 (Contractor visit self-view · thin · smoked):** role `CONTRACTOR` + perm `visitors.self`; `VisitorAppointment.userId` (migration `0020`); `ContractorPortalGuard` allowlist `/visitors/me|/me/appointments|/me/entries` + auth me/change-password; demo `contractor1@vendor.co.tz` → VIS-DEMO-001 (APPROVED) + VIS-DEMO-005 (REJECTED). **API:** contractor self GETs (no plain gate code); staff routes stay `visitors.manage`. **Smoke:** own refs 200; staff list/approve/gate 403; Jane/owner `/visitors/me` 403; `gate1` staff list 200; UI `/contractor/login` 200. **UI:** visitor-web `/contractor/login` + `/contractor`. Deferred: self-book authenticated create, invite UX, contractor CRM, parking PermitType.CONTRACTOR self-view, new app
- **§4 External E5 (Consultant visit self-view · thin · smoked):** role `CONSULTANT` + perm `consultants.self` (distinct from `visitors.self`); reuses `VisitorAppointment.userId` (no new migration); `ConsultantPortalGuard` same allowlist as E4; demo `consultant1@auditpartners.tz` → VIS-DEMO-002/003; `/visitors/me*` gated `RequireAnyPermissions(visitors.self|consultants.self|providers.self)`. **Smoke:** own refs 200; staff/approve/gate 403; contractor still own 001/005; Jane 403; gate1 200; UI login 200. **UI:** visitor-web `/consultant/login` + `/consultant`. Deferred: invite/bind UX, CRM, authenticated self-book
- **§4 External E6 (Service-provider visit self-view · thin · smoked):** role `SERVICE_PROVIDER` + perm `providers.self` (distinct from `visitors.self` / `consultants.self`); reuses `VisitorAppointment.userId` (no new migration); `ServiceProviderPortalGuard` same allowlist as E4/E5; demo `provider1@techcare.tz` → VIS-DEMO-008 (APPROVED) + VIS-DEMO-009 (REJECTED). **Smoke:** own refs 200; staff/approve/gate 403 `SERVICE_PROVIDER_PATH_DENIED`; contractor 001/005 + consultant 002/003 unchanged; Jane 403; gate1 200; UI `/provider/login` 200. **UI:** visitor-web `/provider/login` + `/provider` (cookies `pssms_provider_*`). Deferred: invite/bind UX, CRM, authenticated self-book, dedicated app
- **§4 User Categories Phase 7 (ABAC viewing hierarchy · thin):** **Default-deny** site scope via `siteScopeWhere` / `assertSiteAccess` / `assertBranchAccess` (`libs/shared/utils/site-scope.util.ts`) — only `ORG_WIDE_SITE_VIEW_ROLES` bypass; others use JWT `allowedSiteIds` (empty = see nothing). **Seed:** `supervisor1`+`field1`+`guard1` → `SITE-WAREHOUSE-A`; `bom1` → `UserBranchAccess` DSM-HQ only (**A7** expands sites at auth). **Lists/mutates:** sites (+ create branch-gated), gates, attendance (+ approve / supervisor clock-in), alertness schedule/miss/pending (manage), field-alerts (+ escalate/ack), deployments (+ end), shifts/checkpoints/patrol-routes/scans, incidents, EOB. Deferred §4: staff customer/contract list gates
- Approvals engine + audit log API
- Guards/HR basics, operations (deployments/shifts/checkpoints); **Guards API** `GET /guards` (ops|guards.manage) + `POST /guards` + `PATCH …/status` + **`PATCH …/readiness`** (G3) + linkable → `guards.manage` — list enriched with phone/photoUrl/createdAt, readiness flags (`trainingCompleted`/`clearanceVerified`/`firearmAuthorized`/`firearmExpiry`), linked employee `{employeeId,fullName}`, latest ACTIVE deployment + org site code/name; SUSPENDED/TERMINATED force `deploymentEligible: false`; **incomplete checklist does not hard-block deployable**
- Attendance clock + alertness + patrols APIs; field sync; **alertness** schedule (org-validated) + confirm + pending; **auto-schedule on clock-in** (interval default 120m + randomize jitter; skip if upcoming exists; `ALERTNESS_AUTO_SCHEDULE_ON_CLOCK_IN`); **mark missed** / **scan-missed** → `FieldAlert` `ALERTNESS_MISSED` + outbox; worker `ALERTNESS_MISS_SCAN_ENABLED`
- Visitors appointments/codes/gate verify; access-control; parking + ANPR result path
- Recruitment postings/apply; payroll cycles/snapshots; loans API
- Finance invoices/petty cash/vouchers; procurement/inventory/assets APIs; **invoices** validate optional `contractId` (same customer/org; APPROVED|ACTIVE|EXPIRING), return `contractNumber`, list `?contractId=`; **void** unpaid DRAFT/SENT/OVERDUE (`POST …/:id/void`); **overdue scan** SENT/PARTIALLY_PAID past due → OVERDUE (`POST …/scan-overdue` + internal + worker `INVOICE_OVERDUE_SCAN_ENABLED`)
- Devices registry/ingest/commands + iClock; notifications + outbox worker; **list/gateways ops-enriched** `siteCode`/`siteName` (org-scoped site batch; `operations.manage`; CCTV `config` unchanged)
- Reporting KPIs/exports service; admin/customer/supplier/parking/visitor/recruitment UIs
- **Parking `parking-web` :3006 (thin+ · Portal 35.12 + E3 owner + 13-A RFID + 13-B billing):** ops login `parking1`; permits/ANPR decide/entries/violations/blacklist APIs real; **Vehicles `/vehicles`** list + Edit RFID modal (`Vehicle.rfidTagRef` · Module 13-A); **Permits** fee + Bill → DRAFT invoice (Module 13-B); **ParkingShell** mirrors AdminShell (navy top bar, collapsible sidebar, colored ServiceIcons, grouped nav Overview/Access/Enforcement, module search); **Dashboard control-room** — KPI strip, site permit layout tiles (ACTIVE permits by site + colored vehicle-type filter), ops overview bars, ANPR queue + recent entries/violations; seed 10× CAR/MOTORCYCLE/TRUCK/BUS (+ T123ABC `RFID-DEMO-T123`). **List UIs redesigned** (cards/table, filters, readable dates, site/vehicle enrich): `/vehicles` · `/permits` approve/reject · `/entries` · `/anpr` Allow/Deny · `/violations` · **`/blacklist`** dark Add form + KPIs + search/status/type filters + deactivate + demo seed (~10 plates). **E3 owner:** `/owner/login` + `/owner` read-only my vehicles/permits/entries (+ RFID chip when set). Deferred: stall maps, billing (→ 13-B), MQTT RFID barriers, Nest video
- Mobile guard / gate / supervisor v1

## Thin / partial UI vs design

- **Executive `executive-web` :3011 (thin · Portal 35.2):** navy control-room dashboard; period presets Today/7d/MTD/Custom + Apply; role greeting (CEO/CMD/GM/…); spotlight KPIs; category left-list → right analysis + **live site drill-down** (`GET /reporting/kpis/:code/drilldown`, real `groupBy` counts — no fake trends); session refresh on 401; exports csv/xlsx/pdf. **Authz:** reporting routes gated `reporting.read` (CEO/CMD seed + GM/SUPER_ADMIN allPerms); Guard 403. Login defaults `ceo@highlink.co.tz`. Deferred: charts, risk register, live SSE, ABAC branch filter, fake sparklines
- Marketing ≈ customer directory (no full BD pipeline)
- **Branch Ops `/branch` (thin):** shell + **Overview control-room UI** (navy/teal hero Portal 35.23 / §27, colored KPI cards, quick-link grid, branches table, optional ACTIVE deployments strip, refresh + error banner, honest deferral footer — live from `listBranches`/`listSites`/`listDeployments`/`listShifts`/`listFieldAlerts`); sub-routes still thin as listed. **Sites** create/list (`enterprise.manage` OR `operations.manage`); **Deployments** create (G2: required billable `contractId` + ContractSite cover; enrich `contractNumber`) + **end** (`POST …/deployments/:id/end`, audit `deployment.ended`, overlapping ACTIVE rejected); **Shifts** create/list; **Attendance board** `/branch/attendance` — today’s clock + **A1 supervisor Approve** (`POST /attendance/:id/approve`, filter Pending/Approved, SoD guard≠approver, api-client `approveAttendance`) + **A2 supervisor clock-in** (`POST /attendance/supervisor-clock-in`, method SUPERVISOR, guard/site select modal, open-attendance 409, audit `attendance.supervisor-clock-in`, alertness auto-schedule; api-client `supervisorClockIn`; supervisor clock-out deferred) + **A3 list enrichment** (`GET /attendance`: `clockInMethod`/`clockOutMethod`, `geofenceWarning` from remarks, compute-only `isLate`/`lateMinutes`/`isOvertime`/`overtimeMinutes` vs linked shift; branch board Method/Late/OT/Warning columns + geofence filter chip; no payroll persist) + **Alertness (§10 thin + AL1)**: Schedule check modal, Mark missed, Scan missed (past-due SCHEDULED → MISSED + FieldAlert `escalationStage=SUPERVISOR`); pending overdue highlight; open field alerts + **AL1 escalate** (`POST …/field-alerts/:id/escalate` SUPERVISOR→FIELD→BOM→CONTROL; GUARD-only 403; early ack at any stage; audit `field_alert.escalated`; migration `0015_field_alert_escalation`) + ack; Stage column + Escalate on `/branch/attendance` + `/branch/alerts`; seed `ALT-DEMO-PENDING` (past due) + `ALT-DEMO-FUTURE` + FieldAlert. Deferred vs §10: per-site `AlertnessSchedule` master data UI, face/QR/selfie verify, payroll/discipline hooks. **Field alerts** list + escalate + acknowledge (`?escalationStage=` filter). **EOB** `/branch/eob` — append-only create/list/correct via `libs/occurrence-book` (`operations.manage`; audit `occurrence.created` / `occurrence.corrected`); **History** modal via `GET /occurrence-book/:id/history` (full lineage; Approved / Pending approval); **Approve** `POST /occurrence-book/:id/approve` (current + `approvedBy` null; recorder ≠ approver SoD; audit `occurrence.approved`; originals + corrections); **Attachments** modal via `libs/documents` + MinIO (`documents.manage`; upload/list/presign; audit `document.uploaded`; pdf/png/jpeg/webp ≤10MB); site filter; seed demo ×2 at SITE-WAREHOUSE-A. **Patrols** `/branch/patrols` — **Routes** create/list (`POST/GET /operations/patrol-routes`) with ordered checkpoints + today’s coverage NOT_STARTED/IN_PROGRESS/COMPLETED + **A4a SLA** `slaStatus` OK/ON_TRACK/LATE/MISSED (`dueMinutesFromMidnight`, default 23:00); `POST …/patrol-routes/scan-missed` + `POST …/:id/mark-missed` → FieldAlert `PATROL_MISSED` (AL1 escalate on `/branch/alerts`); worker `PATROL_MISS_SCAN_ENABLED`; migration `0016_patrol_route_sla`; seed route dueMinutes=0 + demo alert. Checkpoints create/list; **Scans** list-only; field scan `POST …/patrols/scan`. Deferred: GPS map, on-spot problem→incident, mobile route picker, full schedule master. **Incidents** `/branch/incidents` — create/list + **A4b** role-gated escalate OPEN→INVESTIGATING→RESOLVED→CLOSED (`allowedNextStatuses`/`blockedReason`; SUPERVISOR can investigate/resolve; CLOSE needs GM+; CRITICAL close GM/CEO; reporter≠closer SoD; Guard create-only on status; audit `incident.status.*`); seed INC-DEMO-00004 CRITICAL RESOLVED. Deferred: full CEO matrix UX, risk register, CCTV link, attachments. `/operations` remains guard readiness console. Deferred: Live SSE, route builder UI, GPS map, missed-patrol alerts matrix, mobile offline patrol, full incident escalation matrix, risk register, CCTV link, incident attachments (reuse documents later), parking, branch petty cash UI, gates CRUD UI, ABAC branch filter, full EOB approvals-lib workflow / mobile EOB, virus scan / delete workflow / rich search, fake KPIs
- **Compliance `/compliance` (thin):** shell — Overview (`audit.read`); **Policies** mutate `compliance.manage` (CO/`compliance1`); **Breaches** mutate `dpo.manage` (DPO/`dpo1`, CISO); breach GET tightened (not bare IT `audit.read`). Prisma `compliance` + RLS; workflow `policy-change-approval` (CO → **GM**); breach close SoD; demo `POL-DPP-001` / `BRCH-00001`. Distinct from ops `SECURITY_BREACH`. Deferred §28/§32: risk/DPIA/consent/DR, archive-via-approval
- **CCTV `/cctv` (thin control-room):** **UI** monitoring wall mosaic (1/4/9/16), Connect camera modal, focus drawer, KPI strip, AI inbox + ANPR panels (`operations.manage`). **API** `CCTV_CAMERA` + `config` JSON (`streamUrl`/`embedUrl`/`snapshotUrl`/zone…); `GET /devices/events?type=CCTV_EVENT`; seed CAM-GATE-01/YARD/WH/PARK + demo alerts. Tiles: http(s) iframe/embed or NVR placeholder + live pulse — **no Nest video.** Deferred: NVR vendor SDK, MQTT barriers, snapshot virus scan, live SSE mosaic, real vision AI
- **Ops Guards `/operations/guards` (thin control-room · G1 create + G2 contract deploy + G3 readiness done):** navy/teal hero (Guard Mgmt §8), KPI strip Total/Active/Deployable/**Readiness OK**/Suspended, search + filter chips, card/list with readiness badges, Suspend/Reactivate, toggle deployable (`PATCH …/status`; UI warns if checklist incomplete — **no hard-block**), detail drawer readiness checkboxes + firearm expiry (`PATCH …/readiness`, audit `guard.readiness.updated`). **G1 Create:** `POST /guards` + linkable-users picker + New guard modal; **authz harden:** linkable/create reject `customerId`/`supplierId` accounts (§33); GUARD-only JWT cannot create/status/readiness/linkable (still has `operations.manage` for list/mobile). **G2 Deploy↔Contract:** billable `contractId` + ContractSite cover; seed GRD-0001/2/3 → CTR-DEMO-GUARD-2026. **G3 Readiness:** `GuardProfile.trainingCompleted`/`clearanceVerified`/`firearmAuthorized`/`firearmExpiry` (migration `0013_guard_readiness`); seed GRD-0001 training+clearance+firearm+expiry 2030-12-31, GRD-0002 training only, others false. Still deferred: full Employment→CEO matrix, rich training records on profile, firearm license CRUD, equipment (HR + assets)
- **Devices `/devices` (thin control-room):** navy/teal hero (“Field devices”), type chips (Fingerprint/Face/RFID/QR/Printer/CCTV), KPI strip Total/Online/Offline/Gateways, tabs Gateways | Devices | Recent events (`listDeviceEvents` limit 40). Devices: search + status chips + type dropdown, card grid (online pulse, vendor/last seen) + list; drawer + `/devices/[id]`; CCTV_CAMERA → `/cctv`. Gateways cards/table + register modal (one-time API key). New device modal restyled; detail page hero/KPIs + command modal kept. `operations.manage`. Deferred: MQTT Nest bridge, live map, virus scan
- **Developer `/developer` (Phase A–C):** health, adapters (+ ping), webhook/outbox filters+replay, notifications, devices summary, **logs**, **config** (non-secret); RBAC `integrations.manage`; DEVELOPER role seeded. Still missing: real WhatsApp/banks/mobile-money vendors, import/export bulk tools, MQTT Nest bridge
- **HR `/hr` (Phase A–C):** shell + Overview; employees create/edit/list (`hr.manage`); leave types/apply + approve/reject; salary assignments; **training** records; **discipline** cases; **transfer/exit movements** via approvals (`employee-transfer-approval` / `employee-exit-approval`; EXIT → TERMINATED + guard not deployment-eligible + **ACTIVE deployments ended** via `DeploymentsService.endAllActiveForGuard` port; PATCH TERMINATED blocked). Still missing vs §16: performance reviews, employee docs, full Employment→CEO matrix UX
- **ESS `/ess` (Phase A–B):** self-scoped (`ess.access`) — profile, leave, payslips, loans, **petty cash request**, equipment + **return request** (sets `RETURN_PENDING`; storekeeper confirms), **requests inbox** (leave/loan/movement/petty cash); HR can **link userId** for ESS; seed office `OFF-SUP-001` ↔ supervisor1 + demo radio on guard. Still missing: AP payment-voucher self-create (design: accountant-only — deferred)
- **Loans `/loans` (thin admin):** org-wide list/create, approve/reject (`loans.manage`, multi-step-safe + creator≠approver), installment schedule view; ESS self-apply remains `/ess/loans`. Still missing: statements, issue/disbursement UX beyond approve, loan-type catalog
- **Assets `/assets` + returns (thin):** shell Register | Returns; register create/list (`assets.manage`); assign AVAILABLE → employee/guard via `GET /assets/assignee-options` (org-validated); list includes active assignment summary; audit `asset.created` / `asset.assigned`. **Returns `/assets/returns`:** pending ESS queue + storekeeper confirm (condition + receipt; creator≠confirmer); walk-in `POST /assets/:id/return` kept. Deferred: transfer, dispose, maintenance, damage charges, inventory sync, MinIO photos
- **Finance invoices `/finance` (thin):** list/send/pay/void + **New invoice** modal (customer → optional billable contract → line); roster shows `contractNumber`; **Scan overdue** + worker job; seed INV-DEMO-001…006 linked to CTR-DEMO-* (006 past-due SENT for scan). Still deferred: multi-line editor, credit notes, EMAIL overdue alerts
- **Petty cash `/finance/petty-cash` + ESS `/ess/petty-cash` (thin):** employee requests against HQ imprest (fund auto-picked); finance list/approve/reject (`finance.manage` UI; seeded step role `GENERAL_MANAGER` / SUPER_ADMIN; multi-step-safe + creator≠approver; balance checked before final approve/debit); **REIMBURSED** after APPROVED via `POST …/vouchers/:id/reimburse` (receiptUrl and/or notes; creator≠reimburser; audit `petty_cash.voucher.reimbursed`); finance UI Mark reimbursed + status filter; **MinIO receipts** — Receipts modal + reimburse upload/list/presign via `libs/documents` (`PettyCashVoucher`; `documents.manage` + `finance.manage`; stores `receiptUrl` as `document:{id}`); ESS shows REIMBURSED + date + receipt-on-file hint (read-only; no ESS upload — parent authz is finance). AP payment vouchers remain accountant-created. Still deferred: float replenishment, full Supervisor→Finance→GM matrix, branch petty cash UI, ESS self-upload of receipts
- **Documents `/api/v1/documents` (thin):** `libs/documents` + MinIO bucket `pssms-documents` (createIfNotExists); `DocumentObject` metadata in `documents` schema + RLS; upload/list/presign; `documents.manage` on SUPER_ADMIN/GM (allPerms) + SUPERVISOR/GUARD + CALL_CENTRE. **Authz harden:** after org ownership, parent capability required — `OccurrenceEntry` → `operations.manage`, `PettyCashVoucher` → `finance.manage`, `Customer` → `customers.manage`, `Contract` → `contracts.manage`, `VisitorAppointment` → `visitors.manage` (upload/list/presign). Portal read-only on own `Customer` + own `Contract` + own-customer `VisitorAppointment`. Wired on Branch EOB, Finance petty-cash, Super Admin customer drawer, **Contracts Docs**, **Call Centre visitor ID scans**. Deferred: virus scan, soft-delete, CCTV snapshots / payslip buckets, public CDN, public book-time visitor upload
- **Customer portal `customer-web` (thin · Portal 35.8 + E1 35.9):** cookie auth + refresh-on-401; shell mirrors AdminShell; nav Overview/Contracts/Guards/Attendance/Access/**My access**/Visitors/Parking/Incidents/Invoices/Requests/**Complaints**/SLA/**Reports**/Documents/Alerts/**Contacts**/Profile (permission-filtered; employee → My access only). Live R from JWT-scoped APIs + `/customers/me/*` ops (sites/deployments/incidents/attendance-summary). Labels enriched (site/employee/plate). **Attendance** — `GET …/attendance-summary` returns `onDutyNow` + `clockedGuards[]` (latest clock-in per site+guard today); customer-web `/attendance` site cards + guard list, Live/Paused + 30s poll (SSE deferred). **Service requests** — `CustomerServiceRequest` in `customers` schema + RLS; portal `GET/POST /customers/me/service-requests` + cancel OPEN; staff `GET/PATCH /customers/service-requests` (`customers.manage` OR `visitors.manage`); creator≠processor SoD; audit; Call Centre inbox + portal `/requests` create/list; seed `SR-00001`. **Contracts (§7 thin+ · B1 commercial + B2 sites + B3 multi-step approval + C1 lifecycle)** — admin `/superadmin/contracts` create with **multi `serviceTypes`**, editable **currency** (TZS/USD/EUR/KES), **paymentTerms** (NET_*/PREPAID/ON_INVOICE), **contractKind** (NEW/RENEWAL/AMENDMENT), **renewalDate**, **noticePeriodDays**, **invoiceFrequency** (MONTHLY/WEEKLY), **vatApplicable**, **slaLevel** (STANDARD/PREMIUM/CRITICAL), guardCount/slaTerms; roster Kind + Notice columns; `serviceType` kept as primary (first selected); Docs modal (`resourceType=Contract`); Expiring ≤90d filter/KPI; **Approval** — `contract-approval` **B3** Legal→GM→CEO→CMD@10M (`amountThreshold` on CMD step; skipped below threshold): Submit DRAFT→PENDING_APPROVAL, step Approve (role-gated)→APPROVED when matrix complete, Reject→DRAFT; Activate APPROVED→ACTIVE; Terminate ACTIVE; DRAFT→ACTIVE blocked; `approvalInstanceId` + enrichment `approvalStatus`/`approvalCurrentStepOrder`/`approvalCurrentStepName`/`approvalRequiredRole` + creator≠approver SoD; seeded `legal1`/`ceo`/`cmd`/`marketing1`; **Scan expiring** ACTIVE→EXPIRING + EMAIL; worker `ContractExpiryJob`; **commercial-alerts** (staff-only); portal `/documents` contract files + portal contracts show joined `serviceTypes`. **B2 Contract↔Sites** — `ContractSite` (`contracts.contract_sites`); create optional `siteIds` (org+customer validated); list/get enrich `siteIds` + `sites[{id,code,name}]`; `PUT /contracts/:id/sites` DRAFT-only replace + audit `contract.sites.updated`; admin Sites checkboxes (filter `listSites` by customer); table site-code chips; portal cards show sites when present; seed CTR-DEMO-* → SITE-WAREHOUSE-A / SITE-OFFICE-DEMO. **Invoice↔contract** — create validates `contractId` for customer/org + billable status; DTO `contractNumber`; admin `/finance` create picker. **G2 GuardDeployment↔contract** — deploy requires billable contract + ContractSite cover. Deferred: SMS/WhatsApp expiry. **Visitors host approve/reject** — `CustomerPortalGuard` allowlists `POST …/appointments/:id/approve|reject` only; service scopes by `customerId`; creator≠approver SoD; issues verification code + audit. **Super Admin portal invite** — `GET/POST /customers/:id/portal-users` (`customers.manage`); creates `CUSTOMER_PORTAL` + binds `customerId`; one-time temp password (+ EMAIL `CUSTOMER_PORTAL_INVITE` outbox); audit `customer.portal_user.invited`; drawer Invite on `/superadmin/customers`. **Force password change** — `User.mustChangePassword` set on invite; JWT claim + portal guard blocks APIs until `POST /auth/change-password`; customer-web `/change-password` gate. **Customer documents** — `resourceType=Customer` on `libs/documents` + MinIO `pssms-documents`; staff upload needs `documents.manage` + `customers.manage`; portal GET list/presign allowlisted + self-scope `resourceId === jwt.customerId` (no upload); admin drawer upload + portal Documents “Shared with you”. Seed `portal@demo-mfg.co.tz` → CUST-DEMO (+ VIS-DEMO-007 pending). Deferred: notifications inbox (real outbox), SLA analytics beyond contract terms, portal user suspend from drawer, fake KPIs, ticket attachments / SMS status alerts

- **Visitor Appointment `visitor-web` :3005 (thin · Portal 35.10 + E4/E5/E6):** public book → reference only (no gate code on success). `GET /visitors/public-config` uses org RLS context (demo HIGHLINK / CUST-DEMO / SITE-WAREHOUSE-A). Public `POST /visitors/appointments` (+ optional **12-D** idType/idNumber); host approve/reject issues hashed code (+ EMAIL/SMS/WhatsApp enqueue · **12-C**); gate `POST /visitors/gate/verify` (ALLOWED / DENIED_EXPIRED / DENIED_ALREADY_USED / DENIED_INVALID; ID refs on allow · **12-D**; host notify on matched deny · **12-E**); portal JWT cannot gate-verify. Optional Bearer on `@Public` create records `createdBy` for SoD (creator≠approver). UI: full-width sidebar form (+ ID type/number); success shows boarding-pass **visitor pass** (landscape + mobile portrait) with QR of reference + **Download** dropdown (Image PNG / PDF icons); gate code never on card. **E4 contractor** `/contractor/*` (`contractor1@`); **E5 consultant** `/consultant/*` (`consultant1@auditpartners.tz`); **E6 provider** `/provider/*` (`provider1@techcare.tz`). **12-F** ID scans attach via Call Centre / staff MinIO (portal host list/presign). Deferred: multi-customer picker, seeded scannable gate codes, authenticated self-book create, invite UX, public book-time ID upload, real Meta WhatsApp API
- B2B other-security: thin E2 done (partner routes on recruitment-web); not a dedicated mature app / invoice / hire pipeline
- Guard mobile: no full face/NFC/QR stack yet

## Empty or deferred (known)

| Gap | Notes |
|---|---|
| `libs/compliance` | Partially filled — policies + breach register + audit overview; risk/DPIA/consent/DR still empty |
| `libs/documents` | Filled thin (metadata + MinIO); payslip/CCTV buckets & delete deferred |
| RabbitMQ publish | Infra only; app uses DB outbox + HTTP |
| MQTT Nest bridge | EMQX up; realtime is SSE only today |
| Vision/analytics AI | Stub algorithms |
| Console SMS/payment | Swap for real providers via integration-gateway |
| Social login buttons | Admin “coming soon” |

## Local Docker host remaps (busy machines)

Internal container ports unchanged. Host remaps (busy-machine / bitmeme clash): Postgres **5434**, Redis **6381**, MinIO **9010/9011**, admin-web **3020**, core-api **4101**, background-worker **4102**, Keycloak **8180**, executive-web **3011**. See compose `*_HOST_PORT` + `.env`.

## Careful backlog (A–H — see `pssms-careful-delivery`)

Work **one slice at a time**; prove API + UI before the next. **Careful backlog A–H is complete.**

| # | Slice | Status |
|---|---|---|
| A | Assets register + assign (admin) | **DONE** |
| B | Petty cash receipt → REIMBURSED | **DONE** |
| C | EOB history UI (lineage) | **DONE** |
| D | EOB second-person approve (corrector ≠ approver) | **DONE** |
| E | Attachments / `libs/documents` + MinIO | **DONE** |
| F | Patrols thin UI | **DONE** |
| G | Incidents thin UI | **DONE** |
| H | Petty cash MinIO receipt UI | **DONE** |

## Agent habit

Before implementing: name module + portal + owning lib/app from maps; check this status + `pssms-careful-delivery` so you extend real code instead of duplicating scaffolds.
