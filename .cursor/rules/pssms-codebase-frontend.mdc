---
description: Implemented frontend apps, admin portals, UI kit, mobile, API client patterns
alwaysApply: true
---

# Frontend, UI & Mobile Map (as implemented)

Monorepo: `frontend/` — Next.js 15 + React 19 + Tailwind 3. Design: Azure/Office blues (`#0078d4`), Outfit, Lucide, Recharts. Shared UI: `@pssms/ui` (Shell, AdminShell, DataTable, KPI/console/azure tiles) — **not** shadcn/MUI.

## Next.js apps

| App | Port* | Purpose | Notes |
|---|---|---|---|
| `admin-web` | 3000 | Internal multi-portal | Cookie auth + optional OIDC; middleware |
| `executive-web` | 3001† | Executive KPIs | sessionStorage auth; no middleware |
| `customer-web` | 3002 | Customer portal (35.8 + 35.9) | Cookie auth; own-data only |
| `supplier-web` | 3003 | Supplier portal (35.17) | Cookie auth; own supplier; messages + issued POs |
| `recruitment-web` | 3004 | Public jobs/apply + applicant status (35.13); B2B partner is 35.14 | No login on careers |
| `visitor-web` | 3005 | Visitor appointment (35.10) | Public book + E4/E5/E6 logins |
| `parking-web` | 3006 | Parking ops | Cookie auth |

\*Canonical listen ports. †Docker host may remap (e.g. executive **3011**).  
`public-web` (:3007) planned — not required for core ops.

## admin-web portals (`app/(portals)/` + `@pssms/permissions` `ADMIN_PORTALS`)

`/superadmin` (+ customers/contracts) · `/administration` · `/hr` (+ onboarding) · `/ess` (+ attendance/training/notices/approvals) · `/loans` · `/payroll` (+ cycles/alerts/reports) · `/finance` (+ invoices/vouchers/reports) · `/procurement` (+ buying/inventory) · `/operations` (+ guards) · `/cctv` (+ alerts/parking/access/patrols/alarms/incidents) · `/devices` · `/branch` (+ staff/inspections/parking/petty-cash + reports) · `/compliance` (+ policies/consents/breaches/risks/access/incidents/legal) · `/approvals` · `/callcentre` (+ visitors/complaints/tickets) · `/marketing` (+ pipeline/campaigns/commissions) · `/developer` (+ apis/systems/export)  

Gated by permissions; `SUPER_ADMIN` sees all. Internal design portals live as **routes here**, not separate Nest services.

## Packages

- `@pssms/api-client` — fetch to `NEXT_PUBLIC_CORE_API_URL` (default `:4001`) + reporting `:4005`; modules admin/hr/ess/loans/customer/supplier/visitor/recruitment/parking-ops/developer
- `@pssms/auth` — cookie sessions (admin/customer/supplier/parking) + OIDC PKCE
- `@pssms/permissions` — `can()`, `navForUser()`, portal catalogs
- `@pssms/ui` — shared shells/tables/tiles

## Mobile (`mobile/` Expo)

| App | Phase note | Job |
|---|---|---|
| `guard-app` | Phase 12 / Portal 35.6 | Duty site/shift, clock, alertness, patrol, parking patrol, incident, emergency, kit confirm, notices, SQLite outbox |
| `gate-verification-app` | Phase 13 v1 | Visitor OTP verify (online) |
| `supervisor-app` | Phase 13b / Portal 35.7 | Live board + site picker; attendance approve/clock-in; shifts confirm/replace; EOB inspect + shift-report approve; alertness; incidents resolve/close; patrols; deployments; field-alert escalate (SUPERVISOR / FIELD / BOM / Ops Mgr) |

Mobile uses `EXPO_PUBLIC_API_BASE` → core-api (not the TS api-client package).

## UI coding rules

Match existing `@pssms/ui` + Tailwind patterns; extend api-client module for the portal; wire permissions before adding nav items; do not spawn a new Next app for an internal portal that belongs in admin-web.
